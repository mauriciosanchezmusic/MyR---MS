%% PROGRAMA DE PRUEBA DE MyR
%%  - Programa para probar el compilador y máquina virtual usando operaciones aritméticas

program aritmetica;

var
    int i,j,p;
    float valor;

main() {
    write("valor de p es");
    read(p);
    j = p * 2;
    write("Resultado de j es", j);
    valor = j / p;
    write("Resultado de valor es",valor);
}
