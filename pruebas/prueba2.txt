%% PROGRAMA DE PRUEBA DE MyR
%%      Probar el compilador usando el ciclo while

program ciclos1;

var
    int i,j;
    char f;

main() {
    i=5;
    j = 10;
    read(f);
    while(i < j) do {
        write(f);
        i = i + 1;
    }
}
